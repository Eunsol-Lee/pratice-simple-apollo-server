This is the simplest apollo server

You can test this by opening playground: http://localhost:4000/ or by command this `curl -X POST "http://localhost:4000/" -H "content-type: application/json" -d '{"query": "{ping}"}'`
